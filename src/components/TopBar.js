import React from 'react';

const TopBar = () => {
    const mainDiv = 
    <article id="topbar">
        <section className="title">
            Danny's Pathfinder
        </section>
    </article>
    return mainDiv
}

export default TopBar;