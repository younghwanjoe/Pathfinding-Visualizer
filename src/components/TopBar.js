import React from 'react';

const TopBar = () => {
  const mainDiv = (
    <article id="topbar">
      <section className="title">Pathfinding Visualizer</section>
    </article>
  );
  return mainDiv;
};

export default TopBar;
